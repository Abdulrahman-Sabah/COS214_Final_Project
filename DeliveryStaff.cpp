#include "DeliveryStaff.h"
#include "Command.h"
#include <iostream>
 void DeliveryStaff::handleRequest(Command* r)
 {
    
    // if (r && r->getType() == "ScheduleDelivery") //Needs to be merged to have command class
    // {
    //     std::cout << "Delivery Staff is scheduling the delivery." << std::endl;
    // }
    // else{
    //     // Pass to next handler in chain using protected method
    //     passToSuccessor(r);
    // }
    
}